# Papeis

Realm: `lotoanalytics`

Papeis da aplicacao:

- `administrador`

Regra local:

- Usuarios em `AdminUserEmails` no `lotoanalytics.start.ps1` recebem `administrador`.

Emails locais atuais:

- Premium: `dev@lotoanalytics.local`, `juliermecarvalho@gmail.com`
- Administrador: `juliermecarvalho@gmail.com`

O único papel funcional da aplicação é `administrador`. A geração de jogos permanece pública e não depende de papel.
